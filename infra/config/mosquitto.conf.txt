# Listener: aceptar conexiones desde cualquier IP en el puerto 1883
listener 1883 0.0.0.0

# Requerir autenticación
allow_anonymous true

# Persistencia: guardar mensajes entre reinicios del broker
persistence true
persistence_location C:\Program Files\mosquitto\

# Logs visibles en la consola si lo corrés a mano
log_type error
log_type warning
log_type notice
log_type information